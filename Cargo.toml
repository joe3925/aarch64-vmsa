cargo-features = ["profile-rustflags"]

[package]
name = "aarch64-vmsa"
version = "0.1.0"
edition = "2024"

[lib]
path = "lib.rs"

[dependencies]
portable-atomic = { version = "1.13.1", default-features = false }
